<?php
// Prepare form
$form->setAttribute('action', $this->url(null, array(), null, true));
$form->setAttribute('class', 'form-horizontal');

$camelFilter = new Zend\Filter\Word\CamelCaseToSeparator;
$title       = ucfirst($camelFilter->filter($entityType));
?>
<h1 class="page-header">
    Editing <?php echo $title ?>: <em><?php echo $entity ?></em>
</h1>
<?php
// Display form
$cancel = $form->get('buttons')->get('cancel');
$cancel->setAttribute('href', $this->url($indexRoute));
echo $this->form($form);
